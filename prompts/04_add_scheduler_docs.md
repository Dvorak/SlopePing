# 04 Add Scheduler Docs

- Document scheduler requirements and cron-like behavior.
- Use `APScheduler` to schedule periodic checks.
- Describe how to configure `CHECK_CRON` in `.env` for custom timing.
- Provide examples for daily, hourly, and weekday-only schedules.